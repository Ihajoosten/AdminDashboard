-   Make register function on register modal
-   Make reset password on reset password modal
-   Make reset password modal
-   Apply proper form validation on client side and make it work on server side (return response)
-   Make a nice professional favicon icon for the web app
-   Restructure routing with child routing
-   Style the login modal
-   Style the register modal
-   Style the reset password modal
-   Make a footer
-   If user is signed in then display the account button if not the login/register buttons